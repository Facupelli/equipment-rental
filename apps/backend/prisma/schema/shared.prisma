enum TrackingMode {
  IDENTIFIED
  POOLED
}

enum RentalItemKind {
  PRIMARY
  ACCESSORY
}

enum FulfillmentMethod {
  PICKUP
  DELIVERY
}

enum CustomDomainStatus {
  PENDING
  ACTIVE
  ACTION_REQUIRED
  FAILED
}
